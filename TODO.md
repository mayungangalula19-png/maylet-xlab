- [ ] Locate the “Create Project” UI styles that currently use white color
- [ ] Remove/replace any white (#fff/#ffffff or rgba(...,255,255,255,...)) usage with the existing accent palette so the user sees consistent theme colors
- [ ] Update the inline `<style>` in `src/modules/projects/pages/CreateProjectPage.tsx`
- [ ] Run a quick build/typecheck (if available) to ensure no TS/CSS issues

