import { createContext, useContext, ReactNode } from 'react'; import { useProjects } from '../hooks/useProjects'; const ProjectContext = createContext({} as any); export const ProjectProvider = ({ children }: { children: ReactNode }) => { const projects = useProjects(); return <ProjectContext.Provider value={projects}>{children}</ProjectContext.Provider>; }; export const useProjectContext = () => useContext(ProjectContext);
