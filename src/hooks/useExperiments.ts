import { useEffect, useState } from 'react'; import { supabase } from '../lib/supabase/client'; export const useExperiments = () => { const [experiments, setExperiments] = useState<any[]>([]); const [loading, setLoading] = useState(true); useEffect(() => { const fetch = async () => { const { data } = await supabase.from('experiments').select('*'); setExperiments(data || []); setLoading(false); }; fetch(); }, []); return { experiments, loading }; };
