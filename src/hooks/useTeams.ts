import { useEffect, useState } from 'react'; import { supabase } from '../lib/supabase/client'; export const useTeams = () => { const [teams, setTeams] = useState<any[]>([]); const [loading, setLoading] = useState(true); useEffect(() => { const fetch = async () => { const { data } = await supabase.from('teams').select('*'); setTeams(data || []); setLoading(false); }; fetch(); }, []); return { teams, loading }; };
